/**
 * A module providing a function to sleep for a given number of milliseconds
 *
 * @example
 * ```ts
 * import { greet } from "jsr:@irbull/sleep@1";
 *
 * await sleep(1000);
 * ```
 *
 * @module
 */

export * from "./sleep.ts";
